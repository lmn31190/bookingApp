{
    "buildCommand" : "cd client && npm install && npm run build",
    "outpoutDirectory" : "client/build",
    "framework" : "react",
    "rewrites" : [
        {
            "source": "/api/(.*)",
            "destination" : "/api/index.js"
        }
    ]
}